-# This file is app/views/movies/index.html.haml

%h2 All Movies

%table#movies
  %thead
    %tr
      %th #
      %th Movie Title
      %th Rating
      %th Release Date
      %th More Info
  %tbody
    -# Commented to be replaced by the parital movie, in order to DRY out the code
    -#- @movies.each_with_index do |movie,index|  
      -#%tr
        %td= index+1
        %td= movie.title
        %td= movie.rating
        %td= movie.release_date
        -#%td= link_to "More about #{movie.title}", movie_path(movie)
        %td= link_to "More about #{movie.title}", movie

    = render :partial => 'movie', :collection => @movies

= link_to 'Add new movie', new_movie_path

%h2 Search TMDb for a movie

= form_tag :action => 'search_tmdb' do

  %label{:for => 'search_terms'} Search Terms
  = text_field_tag 'search_terms'
  = submit_tag 'Search TMDb'
